[user]
  name = Jan Zernisch
  email = jan.zernisch@invision.de
[credential]
  helper = osxkeychain
[push]
  default = simple
[pull]
  rebase = false
[color]
  ui = auto
[alias]
  c = commit
  ca = commit --amend
  co = checkout
  s = status
  d = diff
  l = log
  b = branch
  lp = "!f() { git log --pretty='%h (%cr) - %s' --since=1.week --no-merges; }; f"
  lpm = "!f() { git log --pretty='%h (%cr) - %s' --author='Jan Zernisch'  --since=1.week --no-merges; }; f"
[core]
  excludesfile = ~/.gitignore
  autocrlf = input
[include]
  path = .gitconfig.local
[commit]
  template = ~/.gitmessage
[fetch]
  prune = true
